name: AI CD

on:
  workflow_dispatch:
  push:
    branches: [develop, 'release/**', main]
    paths-ignore:
      - "**.md"
      - ".github/**"
      - ".git*"

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: ${{ vars.AWS_REGION }}

jobs:
  # ----------------------------
  # 1. Build & Push (Matrix x5)
  # ----------------------------
  build-push:
    name: Build & Push (${{ matrix.service }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - service: api_server
            ecr_repo: scuad-ai-api
            dockerfile: ./Dockerfile
            target: runtime
          - service: worker_resume
            ecr_repo: scuad-ai-worker-resume
            dockerfile: ./pipelines/resume_analysis/Dockerfile
            target: production
          - service: worker_job
            ecr_repo: scuad-ai-worker-job
            dockerfile: ./pipelines/job_analysis/Dockerfile
            target: production
          - service: worker_evaluate
            ecr_repo: scuad-ai-worker-evaluate
            dockerfile: ./pipelines/applicant_evaluation/Dockerfile
            target: production
          - service: worker_compare
            ecr_repo: scuad-ai-worker-compare
            dockerfile: ./pipelines/candidate_comparison/Dockerfile
            target: production

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set deploy config
        id: config
        run: |
          SHA_SHORT="${GITHUB_SHA::7}"
          BRANCH="${GITHUB_REF_NAME}"

          if [[ "$BRANCH" == release/* ]]; then
            echo "tag_latest=staging-latest" >> $GITHUB_OUTPUT
            echo "tag_sha=staging-${SHA_SHORT}" >> $GITHUB_OUTPUT
          elif [ "$BRANCH" == "main" ]; then
            echo "tag_latest=main-latest" >> $GITHUB_OUTPUT
            echo "tag_sha=main-${SHA_SHORT}" >> $GITHUB_OUTPUT
          else
            echo "tag_latest=develop-latest" >> $GITHUB_OUTPUT
            echo "tag_sha=develop-${SHA_SHORT}" >> $GITHUB_OUTPUT
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          target: ${{ matrix.target }}
          push: true
          platforms: linux/amd64
          tags: |
            ${{ steps.login-ecr.outputs.registry }}/${{ matrix.ecr_repo }}:${{ steps.config.outputs.tag_latest }}
            ${{ steps.login-ecr.outputs.registry }}/${{ matrix.ecr_repo }}:${{ steps.config.outputs.tag_sha }}
          cache-from: type=gha,scope=${{ matrix.service }}
          cache-to: type=gha,mode=max,scope=${{ matrix.service }}

  # ----------------------------
  # 2. Deploy via SSM Run Command
  # ----------------------------
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: [build-push]

    steps:
      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set deploy config
        id: config
        run: |
          SHA_SHORT="${GITHUB_SHA::7}"
          BRANCH="${GITHUB_REF_NAME}"

          if [[ "$BRANCH" == release/* ]]; then
            echo "tag_latest=staging-latest" >> $GITHUB_OUTPUT
            echo "tag_sha=staging-${SHA_SHORT}" >> $GITHUB_OUTPUT
            echo "target_env=staging" >> $GITHUB_OUTPUT
            echo "ssm_env_path=/staging/ai/DOT_ENV" >> $GITHUB_OUTPUT
            echo "env_label=STAGING" >> $GITHUB_OUTPUT
          elif [ "$BRANCH" == "main" ]; then
            echo "tag_latest=main-latest" >> $GITHUB_OUTPUT
            echo "tag_sha=main-${SHA_SHORT}" >> $GITHUB_OUTPUT
            echo "target_env=prod" >> $GITHUB_OUTPUT
            echo "ssm_env_path=/prod/ai/DOT_ENV" >> $GITHUB_OUTPUT
            echo "env_label=PROD" >> $GITHUB_OUTPUT
          else
            echo "tag_latest=develop-latest" >> $GITHUB_OUTPUT
            echo "tag_sha=develop-${SHA_SHORT}" >> $GITHUB_OUTPUT
            echo "target_env=dev" >> $GITHUB_OUTPUT
            echo "ssm_env_path=/dev/ai/DOT_ENV" >> $GITHUB_OUTPUT
            echo "env_label=DEV" >> $GITHUB_OUTPUT
          fi

      - name: Deploy via SSM Run Command
        id: ssm_deploy
        run: |
          REGION="${{ env.AWS_REGION }}"
          REGISTRY="${{ steps.login-ecr.outputs.registry }}"
          SSM_PATH="${{ steps.config.outputs.ssm_env_path }}"
          TAG="${{ steps.config.outputs.tag_latest }}"
          TARGET_ENV="${{ steps.config.outputs.target_env }}"

          PARAMS=$(jq -n \
            --arg region "$REGION" \
            --arg registry "$REGISTRY" \
            --arg ssm_path "$SSM_PATH" \
            --arg tag "$TAG" \
            '{commands: [
              "set -eux",

              "aws ecr get-login-password --region \($region) | docker login --username AWS --password-stdin \($registry)",

              "mkdir -p /opt/scuad",
              "aws ssm get-parameter --name \($ssm_path) --with-decryption --query Parameter.Value --output text --region \($region) > /opt/scuad/.env",
              "chmod 600 /opt/scuad/.env",

              "docker rm -f scuad-ai || true",

              "docker pull \($registry)/scuad-ai-api:\($tag)",
              "docker pull \($registry)/scuad-ai-worker-resume:\($tag)",
              "docker pull \($registry)/scuad-ai-worker-job:\($tag)",
              "docker pull \($registry)/scuad-ai-worker-evaluate:\($tag)",
              "docker pull \($registry)/scuad-ai-worker-compare:\($tag)",

              "docker rm -f scuad-ai-api scuad-ai-worker-resume scuad-ai-worker-job scuad-ai-worker-evaluate scuad-ai-worker-compare || true",

              "docker run -d --restart unless-stopped --name scuad-ai-api --env-file /opt/scuad/.env -p 8000:8000 \($registry)/scuad-ai-api:\($tag)",
              "docker run -d --restart unless-stopped --name scuad-ai-worker-resume --env-file /opt/scuad/.env \($registry)/scuad-ai-worker-resume:\($tag)",
              "docker run -d --restart unless-stopped --name scuad-ai-worker-job --env-file /opt/scuad/.env \($registry)/scuad-ai-worker-job:\($tag)",
              "docker run -d --restart unless-stopped --name scuad-ai-worker-evaluate --env-file /opt/scuad/.env \($registry)/scuad-ai-worker-evaluate:\($tag)",
              "docker run -d --restart unless-stopped --name scuad-ai-worker-compare --env-file /opt/scuad/.env \($registry)/scuad-ai-worker-compare:\($tag)",

              "docker image prune -f"
            ]}')

          TARGETS=$(jq -n --arg env "$TARGET_ENV" \
            '[{"Key":"tag:Role","Values":["ai"]},{"Key":"tag:Environment","Values":[$env]}]')

          COMMAND_ID=$(aws ssm send-command \
            --document-name "AWS-RunShellScript" \
            --targets "$TARGETS" \
            --parameters "$PARAMS" \
            --timeout-seconds 600 \
            --comment "Deploy AI MSA ${{ steps.config.outputs.tag_sha }}" \
            --query 'Command.CommandId' \
            --output text)

          echo "command_id=$COMMAND_ID" >> $GITHUB_OUTPUT
          echo "SSM Command sent: $COMMAND_ID"

      # ----------------------------
      # 3. SSM 명령 완료 대기
      # ----------------------------
      - name: Wait for SSM Command
        run: |
          COMMAND_ID="${{ steps.ssm_deploy.outputs.command_id }}"

          sleep 10

          for i in $(seq 1 30); do
            RESULT=$(aws ssm list-command-invocations \
              --command-id "$COMMAND_ID" \
              --query 'CommandInvocations[*].[InstanceId,Status]' \
              --output text)

            echo "[$i/30] $RESULT"

            TOTAL=$(echo "$RESULT" | wc -l | tr -d ' ')
            PENDING=$(echo "$RESULT" | grep -c -E 'Pending|InProgress|Delayed' || true)
            FAILED=$(echo "$RESULT" | grep -c -E 'Failed|TimedOut|Cancelled' || true)

            if [ "$TOTAL" -eq 0 ]; then
              echo "No instances matched the target tags"
              exit 1
            fi

            if [ "$PENDING" -eq 0 ]; then
              if [ "$FAILED" -gt 0 ]; then
                echo "===== Failed instance logs ====="
                FAILED_IDS=$(echo "$RESULT" | grep -E 'Failed|TimedOut|Cancelled' | awk '{print $1}')
                for IID in $FAILED_IDS; do
                  echo "--- $IID ---"
                  aws ssm get-command-invocation \
                    --command-id "$COMMAND_ID" \
                    --instance-id "$IID" \
                    --output json || echo "(could not fetch logs)"
                done
                echo "Deployment failed on $FAILED instance(s)"
                exit 1
              fi
              echo "All $TOTAL instance(s) deployed successfully"
              exit 0
            fi

            sleep 15
          done

          echo "Deployment timed out"
          exit 1

      # # ----------------------------
      # # ASG Instance Refresh (Blue-Green) - 추후 전환 시 사용
      # # ----------------------------
      # - name: Start Instance Refresh
      #   id: refresh
      #   run: |
      #     REFRESH_ID=$(aws autoscaling start-instance-refresh \
      #       --auto-scaling-group-name "${{ steps.config.outputs.asg_name }}" \
      #       --preferences '{
      #         "MinHealthyPercentage": 100,
      #         "MaxHealthyPercentage": 200,
      #         "InstanceWarmup": 120
      #       }' \
      #       --query 'InstanceRefreshId' \
      #       --output text)
      #     echo "refresh_id=$REFRESH_ID" >> $GITHUB_OUTPUT
      #
      # - name: Wait for Instance Refresh
      #   run: |
      #     ASG_NAME="${{ steps.config.outputs.asg_name }}"
      #     for i in $(seq 1 20); do
      #       STATUS=$(aws autoscaling describe-instance-refreshes \
      #         --auto-scaling-group-name "$ASG_NAME" \
      #         --instance-refresh-ids "${{ steps.refresh.outputs.refresh_id }}" \
      #         --query 'InstanceRefreshes[0].Status' --output text)
      #       echo "[$i/20] Status: $STATUS"
      #       if [ "$STATUS" == "Successful" ]; then exit 0; fi
      #       if [ "$STATUS" == "Failed" ] || [ "$STATUS" == "Cancelled" ]; then exit 1; fi
      #       sleep 30
      #     done
      #     exit 1

      # ----------------------------
      # 4. Discord 알림
      # ----------------------------
      - name: Notify Discord - Success
        if: success()
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          status: success
          title: "[${{ steps.config.outputs.env_label }}] AI MSA 배포 성공"
          description: |
            Tag: `${{ steps.config.outputs.tag_sha }}`
            Services: api, worker_resume, worker_job, worker_evaluate, worker_compare
            SSM Command: `${{ steps.ssm_deploy.outputs.command_id }}`

      - name: Notify Discord - Failure
        if: failure()
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          status: failure
          title: "[${{ steps.config.outputs.env_label }}] AI MSA 배포 실패"
          description: |
            Tag: `${{ steps.config.outputs.tag_sha }}`
            SSM Command: `${{ steps.ssm_deploy.outputs.command_id }}`
